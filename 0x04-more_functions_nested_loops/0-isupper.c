#include <stdio.h>
#include "main.h"

/**
 * _isupper function checks for uppercase characters
 *
 * returns 1 if c is uppercase and 0 otherwise
 */
int_isupper(int c);
{
	char c;

	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
